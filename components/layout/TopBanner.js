const TopBanner = ({children}) => {
  return (
    <div className="w-full h-4 bg-illegalBlack text-white text-xs flex items-center justify-center rounded-sm">{children}</div>
  )
}

export default TopBanner